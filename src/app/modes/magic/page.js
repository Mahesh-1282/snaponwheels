import MagicContent from "@/components/modes/MagicContent";

export const metadata = {
    title: 'Magic Snap - SnapOnWheels',
    description: 'Step into AR with Magic Snap using live filters and color effects for a unique photo experience.',
};

export default function MagicMode() {
    return <MagicContent />;
}
