import PassportContent from "@/components/modes/PassportContent";

export const metadata = {
    title: 'Passport Mode - SnapOnWheels',
    description: 'Get AI-guided official passport photos. Automatic background removal and 8 verified copies.',
};

export default function PassportMode() {
    return <PassportContent />;
}
