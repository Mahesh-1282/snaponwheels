import SelfieContent from "@/components/modes/SelfieContent";

export const metadata = {
    title: 'Selfie Mode - SnapOnWheels',
    description: 'The classic photobooth experience. Capture 4 sequential poses in color or black & white strips.',
};

export default function SelfieMode() {
    return <SelfieContent />;
}
